# Claude visual theme

Persistent DSH web client styling based on computed styles sampled from a logged-in Claude page through Playwright CDP.

The package changes typography, colors, borders, shadows, and primary-control states. It does not replace shell layout or conversation behavior.
